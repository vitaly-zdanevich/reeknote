# rnsync

Evernote note synchronization CLI from the Reeknote project.

```sh
npm install -g @vitaly-zdanevich/rnsync
```

This package installs the `rnsync` command. Current npm binaries are available
for Linux x64 and Linux ARM64.
